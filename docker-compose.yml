services:
  foodshare-ai:
    build: .
    env_file:
      - .env.local
    ports:
      - "127.0.0.1:3003:3000"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - cloudflared

networks:
  cloudflared:
    external: true
