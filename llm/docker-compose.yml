services:
  ollama:
    image: ollama/ollama
    volumes:
      - ./models:/root/.ollama/models

  llm-proxy:
    build: .
    ports:
      - "152.53.136.84:11434:11434"
    environment:
      - OLLAMA_UPSTREAM=http://ollama:11434
    depends_on:
      - ollama
    restart: unless-stopped
